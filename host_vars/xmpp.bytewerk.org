#postgresql:	# Optional
#  databasename: prosody
#  username: prosody
#  password: 5xyfq6gilJvtBJMiQt4L90X2e	# wird automatisch ermittelt


# Optional. If none is given try to reuse the current one, if that's not possible create a random one
#turnserver:
#  shared_password: kjhsd8721hkqhwdXXX

prosody:
  # Optional: Aufbewahrungsdauer für Nachrichten und Uploads einschränken, Standard: 365 Tage
  delete_uploads_after_days: 31
  delete_messages_after_days: 31

  # Optional, nur nötig wenn bei einer xmpp_domain "authentication_provider = imap" gesetzt wird
  imap_auth:
    host: mail.bingo-ev.de
    port: 993

  web_domain:
    name: "xmpp.bytewerk.org"
    admin_email: "webmaster@bingo-ev.de"
    content_git:	# optional
      url: http://git.bingo-ev.de/geierb/bytewerk-xmpp-server-website.git
      branch: master

  xmpp_domains:
    - name: "bytewerk.org"

      # Components
      muc: "conference.bytewerk.org"
      proxy65: "proxy.bytewerk.org"
      pubsub: "pubsub.bytewerk.org"
      upload: "upload.bytewerk.org"

      legacy_ssl_port: 5223

      allow_registration: false	# Optional, Standard: false

      admin_jids:
        - sqozz@bytewerk.org
        - cfr34k@bytewerk.org
        - geierb@jabber.geierb.de
        - bg3992@bingo-ev.de
      admin_group: "bytewerk"	# Optional: Alles Admins automatischer zu dieser Gruppe hinzufügen

      conversejs:
        weblocation: /conversejs-bytewerk

      testuser:	# Optional
        name: "nrpe-testuser"
#        password: "HOSTVARS-BYTEWERK" # Optional (default: random)

    - name: "bingo-ev.de"

      # Components
      muc: "conference.bingo-ev.de"
      proxy65: "proxy65.bingo-ev.de"
      pubsub: "pubsub.bingo-ev.de"
      upload: "upload.bingo-ev.de"

      legacy_ssl_port: 5225

      allow_registration: false	# Optional, Standard: false

      authentication_provider: imap

      admin_jids:
        - sqozz@bytewerk.org
        - cfr34k@bytewerk.org
        - geierb@jabber.geierb.de
        - bg3992@bingo-ev.de
      admin_group: "bingo-users"

      conversejs:
        weblocation: /conversejs-bingo

#      testuser:	# Optional - Testuser funktioniert nur wenn authentication_provider was mit "internal ist"
#        name: "nrpe-testuser"
#        password: "HOSTVARS-BINGO" # Optional


conversejs:
  conversejs_release_tag: v6.0.1
  libsignal_release_tag: v1.3.0

nrpe:
  # list hostnames, IPv4 or IPv6 addresses allowed to query the nrpe server
  allowed_hosts:
    - www.geierb.de


ansible_python_interpreter: /usr/bin/python3
